/** MIME types accepted for document upload, and used to determine how an uploaded document is rendered. */
export enum ContentType {
  PDF = 'application/pdf',
  JPEG = 'image/jpeg',
  PNG = 'image/png',
  TIFF = 'image/tiff',
}
